const btn = document.getElementById('btn');
const text = document.getElementById('text');

btn.addEventListener('click', () => {
  const text = document.getElementByld('text');
  text.textContent = 'ボタンをクリックしました';
});
